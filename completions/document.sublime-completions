{
        "scope": "text.plain",

        "completions":
        [
        	{ "trigger": "aws lambda", "contents": "Lambda là dịch vụ Serverless của AWS. có thể chạy đc các function trên Python, NodeJS ...\nCác function Lambda có thể sử dụng các aws-sdk tương ứng để truy cập vào các tài nguyên khác trên AWS.\nCác tài nguyên này phải có ROLE tương ứng" },
            { "trigger": "aws cognito", "contents": "-- Cognito\nCognito bao gồm 2 phần:\n	UserPool: chứa danh sách user và group user và AppClient. Mỗi 1 ứng dụng sẽ tạo một UserPool riêng, AppClient riêng.\n		Trong UserPool có các trigger liên quan đến các action của user trong pool, pre-signup, signin, Pre authentication... Các trigger này sẽ gọi tới các Lambda function tương ứng\n	IdentityPool: Là một kho cung cấp xác thực người dùng, có thể qua Cognito UserPool, Facebook, Google hoặc một bên thứ 3.\n		IdentityPool: là nơi add các role dành cho người dùng sau khi Auth. Các role này cho phép user đã auth có thể sử dụng đc các resource tương ứng.\nCognito SDK:\n	AWS.CognitoIdentity: sử dụng cho IdentityPool, gọi các function để xác thực người dùng\n	AWS.CognitoIdentityServiceProvider: Các API CRUD cho UserPool\n	AWS.CognitoSync: Các API để đồng bộ dữ liệu của cùng 1 user sử dụng trên nhiều thiết bị" },
            { "trigger": "aws dynamodb", "contents": "DynamoDB sử dụng một các Expression để diễn tả các hành động truy vấn cần thiết với DB giống như các biến ,  ... trong MongoDB.\n	Các Expression là các string có cấu trúc chưa các function để thực hiện các truy vấn\n	Tham khảo https://docs.aws.amazon.com/amazondynamodb/latest/developerguide/Expressions.UpdateExpressions.html\n	Scan sẽ trả về tối 1m dữ liệu mỗi lần, và kèm tham số LastEvaluatedKey để xác định query đã tìm đến đâu, để query tiếp thì sử dụng ExclusiveStartKey (sử dụng LastEvaluatedKey) để query tiếp\n	Ngoài ra đối với số lượng bản ghi lớn để tìm kiếm nhanh, thì sử dụng Segment, TotalSegements để tìm kiếm," }
        ]
}